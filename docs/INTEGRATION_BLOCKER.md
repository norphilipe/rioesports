# Integration Blocker

The application can continue development without immediate user intervention.

Real dedicated CS2 match execution will eventually require a server-capable runtime. That environment is not created automatically by the repository or database and will become the next external integration boundary.
