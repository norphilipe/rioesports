import Link from "next/link";
import { createClient } from "@/lib/supabase/server";

export default async function NoticiasPage() {
  const supabase = await createClient();
  const { data: posts } = await supabase.from("news_posts").select("title,slug,excerpt,published_at,created_at").eq("status", "published").order("published_at", { ascending: false }).limit(20);
  const featured = posts?.[0]; const stories = posts?.slice(1) ?? [];
  return <main className="min-h-screen bg-[#050505] text-white"><header className="border-b border-white/10"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-6 py-5 lg:px-8"><Link href="/" className="font-black text-lg">RIO<span className="text-cyan-400">ESPORTS</span></Link><nav className="hidden gap-7 text-sm text-white/55 md:flex"><Link href="/">INÍCIO</Link><Link className="text-white" href="/noticias">NOTÍCIAS</Link><Link href="/matchmaking">MATCHMAKING</Link><Link href="/perfil">PERFIL</Link></nav><Link href="/login" className="rounded-lg border border-white/15 px-4 py-2 text-sm font-semibold">Entrar</Link></div></header><section className="mx-auto max-w-7xl px-6 py-16 lg:px-8"><div className="border-b border-white/10 pb-10"><p className="text-xs font-black uppercase tracking-[0.35em] text-cyan-400">Central editorial</p><h1 className="mt-4 text-5xl font-black sm:text-7xl">NOTÍCIAS<span className="text-cyan-400">.</span></h1><p className="mt-5 text-white/50">Esports, jogos, comunidade e tudo o que movimenta o cenário competitivo.</p></div>{featured ? <section className="border-y border-white/10 py-10"><Link href={`/noticias/${featured.slug}`} className="block rounded-2xl border border-cyan-400/20 bg-cyan-400/[0.06] p-8 transition hover:bg-cyan-400/[0.1]"><p className="text-xs font-bold text-cyan-300">RIO ESPORTS JOURNAL</p><h2 className="mt-5 text-3xl font-black sm:text-5xl">{featured.title}</h2>{featured.excerpt&&<p className="mt-5 max-w-3xl text-white/55">{featured.excerpt}</p>}<p className="mt-8 text-sm font-bold text-cyan-300">LER MATÉRIA →</p></Link></section> : <div className="py-20 text-center text-white/40">Nenhuma notícia publicada ainda.</div>}<section className="py-16"><h2 className="text-3xl font-black">Últimas notícias</h2><div className="mt-8 grid gap-4 md:grid-cols-2">{stories.map((story)=><Link key={story.slug} href={`/noticias/${story.slug}`} className="rounded-xl border border-white/10 bg-white/[0.02] p-6 transition hover:border-cyan-400/35"><p className="text-xs font-bold text-cyan-400">RIO ESPORTS JOURNAL</p><h3 className="mt-5 text-xl font-black">{story.title}</h3>{story.excerpt&&<p className="mt-3 text-sm text-white/45">{story.excerpt}</p>}<p className="mt-6 text-xs font-bold text-white/60">CONTINUAR LENDO →</p></Link>)}</div></section></section></main>;
}